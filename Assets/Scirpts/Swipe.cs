public struct Swipe
{
    public float direction;
    public float length;
    public float duration;
    public Swipe(float direction, float length, float duration)
    {
        this.direction = direction;
        this.length = length;
        this.duration = duration;
    }
}
